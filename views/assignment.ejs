<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Assignment Solver</title>
    <link rel="stylesheet" href="/css/ans.css">
</head>

<body>
    <div class="container">
        <h1>Upload Assignment PDF</h1>

        <form action="/solveAssignment" method="POST" enctype="multipart/form-data">
            <input type="file" name="answerPdf" accept="application/pdf" required>
            <button type="submit">🚀 Solve</button>
        </form>

        <% if (typeof originalText !=='undefined' && typeof evaluation !=='undefined' ) { %>
            <hr>
            <div class="result-section">
                <h2>📄 Extracted Questions:</h2>
                <pre><%= originalText %></pre>

                <h2>🧠 AI-Generated Solutions:</h2>
                <pre><%= evaluation %></pre>

                <% if (downloadAvailable) { %>
                    <form action="/downloadAssignment" method="POST">
                        <input type="hidden" name="originalText" value="<%- originalText.replace(/" /g, '&quot;' ) %>">
                        <input type="hidden" name="solutions" value="<%- evaluation.replace(/" /g, '&quot;' ) %>">
                        <button type="submit">📥 Download PDF</button>
                    </form>
                    <% } %>
            </div>
            <% } %>
    </div>
    
    <script>
        // Drag and drop functionality
        const uploadArea = document.getElementById('uploadArea');
        const pdfInput = document.getElementById('pdfInput');

        uploadArea.addEventListener('dragover', (e) => {
            e.preventDefault();
            uploadArea.classList.add('dragover');
        });

        uploadArea.addEventListener('dragleave', () => {
            uploadArea.classList.remove('dragover');
        });

        uploadArea.addEventListener('drop', (e) => {
            e.preventDefault();
            uploadArea.classList.remove('dragover');
            if (e.dataTransfer.files[0].type === 'application/pdf') {
                pdfInput.files = e.dataTransfer.files;
                document.querySelector('#uploadArea label span').textContent = e.dataTransfer.files[0].name;
            }
        });

        pdfInput.addEventListener('change', () => {
            if (pdfInput.files.length > 0) {
                document.querySelector('#uploadArea label span').textContent = pdfInput.files[0].name;
            }
        });
    </script>
</body>

</html> -->



<!-- 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Assignment Solver</title>
    <link rel="stylesheet" href="assignment.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="container">
        <h1>AI Assignment Solver</h1>
        <p>Upload your assignment questions PDF to get AI-generated solutions</p>
        
        <form action="/solveAssignment" method="POST" enctype="multipart/form-data">        
            <div class="upload-area" id="uploadArea">
                <input type="file" id="pdfInput" name="answerPdf" accept=".pdf" required>
                <label for="pdfInput">
                    <img src="https://cdn-icons-png.flaticon.com/512/337/337946.png" alt="Upload PDF">
                    <span>Choose your assignment PDF file</span>
                    <p>or drag and drop file here</p>
                </label>
            </div>
            
            <button type="submit" class="solve-button">
                <i class="fas fa-robot"></i> Generate Solutions
            </button>
        </form>
        
        <div class="instructions">
            <h2>How it works:</h2>
            <ol>
                <li>Prepare a PDF with your assignment questions</li>
                <li>Upload the PDF file</li>
                <li>Our AI will analyze and solve each question</li>
                <li>Receive detailed solutions you can download</li>
            </ol>
        </div>
    </div>

    <script>
        // Drag and drop functionality
        const uploadArea = document.getElementById('uploadArea');
        const pdfInput = document.getElementById('pdfInput');
        
        uploadArea.addEventListener('dragover', (e) => {
            e.preventDefault();
            uploadArea.classList.add('dragover');
        });
        
        uploadArea.addEventListener('dragleave', () => {
            uploadArea.classList.remove('dragover');
        });
        
        uploadArea.addEventListener('drop', (e) => {
            e.preventDefault();
            uploadArea.classList.remove('dragover');
            if (e.dataTransfer.files[0].type === 'application/pdf') {
                pdfInput.files = e.dataTransfer.files;
                document.querySelector('#uploadArea label span').textContent = e.dataTransfer.files[0].name;
            }
        });
        
        pdfInput.addEventListener('change', () => {
            if (pdfInput.files.length > 0) {
                document.querySelector('#uploadArea label span').textContent = pdfInput.files[0].name;
            }
        });
    </script>
</body>
</html> -->



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PDF Answer Evaluator</title>
    <link rel="stylesheet" href="/css/ans.css">
</head>
<body>
    <div class="container">
        <h1>Assignment Solver</h1>
        <p>Upload your Assignment PDF</p>
        
        <form action="/solveAssignment" method="POST" enctype="multipart/form-data">        
            <div class="upload-area" id="uploadArea">
                <input type="file" id="pdfInput" name="answerPdf" accept=".pdf" required>
                <label for="pdfInput">
                    <img src="https://cdn-icons-png.flaticon.com/512/337/337946.png" alt="Upload PDF">
                    <span>Choose your Q&A PDF file</span>
                    <p>or drag and drop file here</p>
                </label>
            </div>
            
            <button type="submit">Evaluate Answers</button>
        </form>
        
        <div class="instructions">
            <h2>How it works:</h2>
            <ol>
                <li>Prepare a PDF of Assignment</li>
                <li>Upload the PDF file</li>
                <li>Our AI will analyze and solve your assignment</li>
                <li>Receive detailed solutions in a second</li>
            </ol>
        </div>
    </div>

    <script>
        // Drag and drop functionality
        const uploadArea = document.getElementById('uploadArea');
        const pdfInput = document.getElementById('pdfInput');
        
        uploadArea.addEventListener('dragover', (e) => {
            e.preventDefault();
            uploadArea.classList.add('dragover');
        });
        
        uploadArea.addEventListener('dragleave', () => {
            uploadArea.classList.remove('dragover');
        });
        
        uploadArea.addEventListener('drop', (e) => {
            e.preventDefault();
            uploadArea.classList.remove('dragover');
            if (e.dataTransfer.files[0].type === 'application/pdf') {
                pdfInput.files = e.dataTransfer.files;
                document.querySelector('#uploadArea label span').textContent = e.dataTransfer.files[0].name;
            }
        });
        
        pdfInput.addEventListener('change', () => {
            if (pdfInput.files.length > 0) {
                document.querySelector('#uploadArea label span').textContent = pdfInput.files[0].name;
            }
        });
    </script>
</body>
</html>